_schema-version: "3.2"
ID: appminingodata
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: appminingodata-destination-content
  type: com.sap.application.content
  requires:
  - name: appminingodata-destination-service
    parameters:
      content-target: true
  - name: appminingodata-repo-host
    parameters:
      service-key:
        name: appminingodata-repo-host-key
  - name: appminingodata-uaa
    parameters:
      service-key:
        name: appminingodata-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: appminingodata_html_repo_host
          ServiceInstanceName: appminingodata-html5-service
          ServiceKeyName: appminingodata-repo-host-key
          sap.cloud.service: appminingodata
        - Authentication: OAuth2UserTokenExchange
          Name: appminingodata_uaa
          ServiceInstanceName: appminingodata-xsuaa-service
          ServiceKeyName: appminingodata-uaa-key
          sap.cloud.service: appminingodata
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: appminingodata-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: appminingodata-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - appminingodata.zip
      name: appminingodata
      target-path: resources/
- name: appminingodata
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: appminingodata-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: appminingodata-destination-service
    service-plan: lite
- name: appminingodata-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: appminingodata-xsuaa-service
    service-plan: application
- name: appminingodata-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: appminingodata-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
